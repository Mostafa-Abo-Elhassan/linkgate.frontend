@inherits LayoutComponentBase
@inject IJSRuntime JS

<div class="public-layout">

    <header class="public-navbar">
        <nav class="navbar">
            <div class="container">

                <div class="navbar-main">

                    <a class="navbar-brand"
                       href="#hero">
                        LinkGate
                    </a>

                    <button class="navbar-toggle"
                            type="button"
                            aria-label="Toggle navigation"
                            aria-expanded="false"
                            onclick="publicNavbar.toggle()">

                        <span></span>
                        <span></span>
                        <span></span>

                    </button>

                </div>

                <div class="navbar-menu"
                     id="publicNavbar">

                    <ul class="navbar-nav">

                        <li>
                            <a class="nav-link" href="#calculator">
                                حاسبة الأرباح
                            </a>
                        </li>

                      

                     

                        <li class="nav-link">
                            <NavLink class="nav-link" href="/how-it-works" Match="NavLinkMatch.All">بنشتغل إزاي؟</NavLink>
                        </li>
                        <li class="nav-link">
                            <NavLink class="nav-link" href="/earnings" Match="NavLinkMatch.All">نظام الأرباح</NavLink>
                        </li>
                        <li class="nav-link">
                            <NavLink class="nav-link" href="/referral" Match="NavLinkMatch.All">نظام الإحالة</NavLink>
                        </li>
                        <li class="nav-link">
                            <NavLink class="nav-link" href="/safety" Match="NavLinkMatch.All">حسابك في أمان</NavLink>
                            @* <NavLink class="nav-link" href="/safety" Match="NavLinkMatch.All">انت في أمان</NavLink> *@
                        </li>
                        <li class="nav-link">
                            <NavLink class="nav-link" href="/policies" Match="NavLinkMatch.All">الشروط والأحكام</NavLink>
                        </li>

                    </ul>

                    <div class="navbar-actions">

                        <button class="btn btn-secondary"
                                @onclick="ToggleTheme">
                            @(isDark ? "Light" : "Dark")
                        </button>

                        @* Language *@
                        @* 
                    <button class="btn btn-secondary"
                            @onclick="ToggleLanguage">
                        @(isArabic ? "English" : "العربية")
                    </button>
                    *@
                        <a href="/login" class="text-dark fw-bold text-decoration-none login-link">تسجيل الدخول</a>
                        <a href="#application"
                           class="btn btn-primary">
                            ابدأ الآن
                        </a>

                    </div>

                </div>

            </div>
        </nav>
    </header>

    <main>
        @Body
    </main>

    <footer class="public-footer border-top">
        <div class="container py-5">

            <div class="row g-4">

                <div class="col-lg-6">
                    <h5 class="fw-semibold mb-2">
                        LinkGate
                    </h5>

                    <p class="text-muted mb-0">
                        منصة لإدارة وتشغيل حسابات LinkedIn
                        من خلال نظام مؤتمت ومنظم.
                    </p>
                </div>

                <div class="col-6 col-lg-3">
                    <h6 class="fw-semibold">
                        روابط
                    </h6>

                    <div class="d-flex flex-column gap-2">
                        <a href="/referral">برنامج الإحالة</a>
                        <a href="/how-it-works">كيف يعمل؟</a>
                        <a href="/policies">الشروط والسياسات</a>
                    </div>
                </div>

                <div class="col-6 col-lg-3">
                    <h6 class="fw-semibold">
                        تواصل
                    </h6>

                    <a href="/contact">
                        تواصل معنا
                    </a>
                </div>

            </div>

            <hr class="my-4" />

            <div class="d-flex flex-column flex-md-row
                justify-content-between gap-2">

                <small class="text-muted">
                    © @DateTime.Now.Year LinkGate
                </small>

                <small class="text-muted">
                    جميع الحقوق محفوظة
                </small>

            </div>

        </div>
    </footer>

</div>

                    
@code {
    private bool isDark;

    private async Task ToggleTheme()
    {
        isDark = await JS.InvokeAsync<bool>("themeManager.toggle");
    }


    // private bool isArabic = true;

    // private async Task ToggleLanguage()
    // {
    //     isArabic = !isArabic;

    //     await JS.InvokeVoidAsync(
    //         "languageManager.setLanguage",
    //         isArabic ? "ar" : "en");
    // }
}